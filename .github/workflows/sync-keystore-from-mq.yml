name: Sync MQ from dreamcatcher-mq

on:
  repository_dispatch:
    types: [sync-keystore-from-mq]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout DreamCatcher
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout dreamcatcher-mq
        uses: actions/checkout@v4
        with:
          repository: martinambrus/dreamcatcher-mq
          path: dreamcatcher-mq

      - name: Sync MQ directories
        run: |
          set -euo pipefail
          SRC=dreamcatcher-mq
          TARGETS=(
            "control_center/app/MQ"
            "workers/analysis/app/MQ"
            "workers/err_log_writer/app/MQ"
            "workers/link_fix_detector/app/MQ"
          )
          for dir in "${TARGETS[@]}"; do
            if [ -d "$dir" ]; then
              echo "Syncing $dir"
              rsync -a --delete \
                --exclude '.idea' \
                --exclude '.git' \
                --exclude '.github' \
                "$SRC/" "$dir/"
            else
              echo "Skipping missing $dir"
            fi
          done

      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            control_center/app/MQ \
            workers/analysis/app/MQ \
            workers/err_log_writer/app/MQ \
            workers/link_fix_detector/app/MQ
          git commit -m "Sync MQ from dreamcatcher-mq"
          git pull --rebase
          git push
